# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local

# Convex
.convex

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent / AI tooling (Cursor, Claude, Convex skill packs)
.cursor/
.agents/
.claude/
AGENTS.md
CLAUDE.md
skills-lock.json

# Submission working file (local only, not tracked in the repo)
submission.md

# Internal pitch strategy (names judges; local only, not for the public repo)
docs/PRESENTATION.md
